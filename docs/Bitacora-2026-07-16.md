# Bitácora — 2026-07-16 (sesión maratónica de retoma)

Estado al cierre: **server local funcional con paridad CSGO + teamplay nuevo + sonidos custom**. Server DETENIDO al cierre (arrancar con `D:\cs2-stack\start-server.ps1`).

---

## 1. Paridad con el servidor CSGO original (meta rectora — cumplida en su núcleo)

Fuente: clon de `arieladasme/csgo-gungame-plugin` en `F:\git\csgo-gungame-plugin`. Análisis completo en `docs/CS2-GunGame-Paridad-CSGO.md`.

- **Gameplay** portado a `gungame.json` (~27 valores: 3 kills/nivel, KnifePro, multi-nivel ×4 con gravedad 0.5/velocidad 1.5, warmup 30s con HE, AFK 5→spec, voto a 20 niveles del final).
- **Orden de armas**: 37 niveles estilo CS 1.6 en `gungame_weapons.json` (finales taser/incendiaria/HE/cuchillo a 1 kill).
- **Server cfg**: hostname `|| ALPHA - Gungame Chile ||`, 11 bots fill, match cvars en `gamemode_casual_server.cfg` (hook que CS2 ejecuta tras el gamemode — server.cfg solo no gana).
- **Sonidos históricos**: Workshop addon **`gungame_sounds` (ID 3766168370)** compilado por CLI (`resourcecompiler`) con los 17 MP3 originales + 14 soundevents `gg.*` (Winner = 5 pistas aleatorias nativas). `UseSoundEvents: true` → **los sonidos ahora respetan el volumen del cliente** (el comando `play` usaba el canal `snd_toolvolume` — causa del "suena aunque esté muteado").
- **Español**: `lang/es.json` del plugin + `ServerLanguage: es` + es.json para QuakeSounds y GG1MapChooser.

## 2. Incidencias resueltas (2 updates de CS2 en el día)

| Incidencia | Causa | Fix |
|---|---|---|
| "Client not updated" al conectar | Server CS2 desactualizado | `update-server.ps1` creado (SteamCMD + espera StateFlags 4 + re-parche gameinfo.gi + relanzar) |
| Gungame "no activado", casual puro | Update CS2 rompió CSS 1.0.370 (carga muda) | Metamod git1406 + CSS v1.0.371 |
| Kills no subían nivel | `MissingMethodException: CBaseEntity.get_Speed()` — plugin compilado contra API vieja | Recompilado net10.0 + CSS 1.0.371; `Speed`→`VelocityModifier` |
| Doble warmup (1:24 + 25s) | mp_warmuptime del gamemode pisa server.cfg | `gamemode_casual_server.cfg` con `mp_warmuptime 30` |
| Recarga al matar no funcionaba | `SetStateChanged` notificaba `m_pReserveAmmo` en vez de `m_iClip1` | Fix 1 línea — `ReloadWeapon: true` revive |
| Efecto multi-nivel imperceptible | `VelocityModifier` se auto-recupera a 1.0 | Timer 0.25s re-aplicándolo mientras dura el bonus |
| Micrófonos no se abrían al ganar | Solo `sv_full_alltalk` | + `sv_alltalk` |
| **Loop infinito de mapas al final** | GGMC `LastDisconnectedChangeMap: true` + server sin humanos rota mapa → bucle | `false` en GG1MapChooser.json. (El cambio de mapa post-victoria SÍ funcionó — fallback a ar_shoots) |
| "Partida cancelada" en el panel | Match terminaba sin condición de victoria | `mp_winlimit 1` antes de `TerminateRound` → panel de VICTORIA de equipo (verificar en juego) |
| Claves crudas `chat.msg.player` | QuakeSounds sin lang es | es.json creados (QuakeSounds + GGMC) |
| Errores `Database is not ready` | Stats al boot (persiste en v1.2.4) | Pendiente — solo afecta !top/wins |

## 3. Features nuevos

- **MODO TEAMPLAY (CS 1.6 `gg_teamplay`)** — implementado en el fork (5 commits), reglas validadas contra doc oficial AMXX: pozo de kills por equipo, meta = req individual × jugadores (cuchillo ×0.33, HE ×0.50), robo suma req individual al pozo, victoria de EQUIPO real. Config `TeamPlay 0/1/2` + comando `gg_teamplay`. **Validado end-to-end con bots+humano**: T ganó, niveles colectivos OK, mods OK.
- **GGTrails** (`F:\git\gg-extensions\GGTrails\`): estelas de colores en granadas (beams nativos, 8 colores aleatorios). Desplegado.
- **QuakeSounds** (Kandru + su addon 3461824328): ladder UT completo (Double Kill→GODLIKE), firstblood, headshot, humillación, prepare/play. Config es/en.
- **GG1MapChooser v1.8.0**: votemap funcionando; pool: 3 stock ar_* + fy_iceworld (3070238628), fy_snow (3592238209), aim_map (3070549948).
- **Sync a upstream v1.2.4** con fixes locales preservados.
- Herramientas: dotnet-claude-kit instalado; MultiAddonManager v1.5.2.

## 4. Commits (repo cs2-gungame)

Pusheados: sync v1.2.2→paridad→merge v1.2.4 (`..565b7c5`).
**SIN pushear (7)**: los 5 de teamplay + log de progreso + fix victoria/EndGameDelay 20. → `git push origin main` al retomar.

## 5. ⚠️ Config TEMPORAL activa en el server (revertir al terminar pruebas)

| Qué | Ahora | Real |
|---|---|---|
| `gungame_weapons.json` (server) | 7 niveles de prueba | 37 niveles (copiar del repo) |
| `MinKillsPerLevel` (server) | 2 | 3 (repo) |
| `EnableFriendlyFireLevel` (server) | 6 | 36 (repo) |
| `TeamPlay` (server) | 1 | decidir default (repo: 0) |
| `bot_join_after_player` (server.cfg) | 0 | 1 (paridad) |
| `sv_hibernate_when_empty 0` (server.cfg) | temporal | quitar línea |

Restaurar = copiar `cfg_files/csgo/cfg/gungame/*.json` del repo al server + editar server.cfg.

## 6. Pendientes (tareas)

- #4 Sonidos: probar todos in-game (funcionan ✅ según prueba inicial) → cerrar.
- #6 Votemap: validar votación completa con humano votando + mapas workshop descargando.
- #7 Quake sounds: probado parcial → cerrar tras test.
- #8 Plugin extensión GG: winner effects (volar), MVP líder, gg.intro/lead sounds.
- #9 Advertisements en chat.
- #11 Teamplay: probar `gg_teamplay 0` (regresión individual), HUD spawn, reconexión.
- #12 Proyecto HLSW web (análisis en `F:\git\Ideas-Negocio-Gaming.md` junto a stats SaaS).
- Seguridad: revocar Steam Web API key vieja (repo CSGO) + rotar MySQL del respaldo.
- `Database is not ready` al boot (stats) — investigar si molesta.

## 7. Lecciones del día (ya en CLAUDE.md/memoria)

1. Tras update grande de CS2: actualizar Metamod+CSS Y recompilar el plugin (síntomas: log CSS mudo / MissingMethodException por kill).
2. Cvars de partida van en `gamemode_casual_server.cfg`, no en server.cfg.
3. `resourcecompiler.exe` compila addons por CLI (sin GUI) — solo el Publish exige Workshop Manager.
4. GGMC rota mapa con server vacío si `LastDisconnectedChangeMap: true` → bucle en LAN.
5. Sin humanos el warmup nativo no termina — bots solos no validan gameplay.
